# Copy to .env and fill in. .env is gitignored - never commit real values.
# On Windows, load these with the commands in the README instead.

# Primary channel (iPhone). Generate a long, unguessable topic name.
NTFY_TOPIC=mba-m5-CHANGEME

# Optional backup channel.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
